/**
 * Biome Downloader
 *
 * The downloader handles the logic for downloading pre-built Biome binaries
 * so that Biome is available globally even if it is not installed on the
 * user's system.
 */
export class Downloader {}
